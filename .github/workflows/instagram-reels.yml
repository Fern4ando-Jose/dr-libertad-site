name: Instagram Reels — Dr. Libertad

# Pipeline GRÁTIS de Reels (vídeo) para o @drlibertad.
# Renderiza um Reel com Remotion no GitHub Actions (tem Chromium + ffmpeg),
# sobe o mp4 para o Vercel Blob (URL pública) e publica via /api/publish-reel.
#
# Roda 1x/dia às 21:00 UTC (18:00 BRT) + disparo manual (workflow_dispatch).

on:
  schedule:
    - cron: "0 21 * * *" # 21:00 UTC = 18:00 BRT
  workflow_dispatch:
    inputs:
      slot:
        description: "Slot usado para buscar o conteúdo (preview)"
        required: false
        default: "noite"
        type: choice
        options: [manha, tarde, noite]
      publish:
        description: "Publicar no Instagram? (no = só renderiza p/ preview)"
        required: false
        default: "no"
        type: choice
        options: ["no", "yes"]

jobs:
  reel:
    runs-on: ubuntu-latest
    steps:
      # 1. Baixa o código do repositório
      - name: Checkout do repositório
        uses: actions/checkout@v4

      # 2. Configura Node 20 com cache de npm
      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json

      # 3. Instala dependências (inclui Remotion). O Remotion baixa o Chromium
      #    Headless Shell automaticamente no primeiro render.
      - name: Instalar dependências
        run: npm install --no-audit --no-fund

      # 4. Define o slot (do input manual ou "noite" no agendamento)
      - name: Definir slot
        id: slot
        run: |
          SLOT="${{ github.event.inputs.slot }}"
          SLOT="${SLOT:-noite}"
          echo "slot=$SLOT" >> "$GITHUB_OUTPUT"
          echo "Slot escolhido: $SLOT"

      # 5. Busca o conteúdo em /api/publish?preview=1 (gera título/slides/cta + a
      #    ilustração da capa via Claude/fal, SEM publicar) e monta o REEL_PROPS.
      - name: Obter conteúdo (preview) e montar REEL_PROPS
        id: content
        run: |
          curl -fsS --max-time 180 \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "https://www.drlibertad.com/api/publish?preview=1&slot=${{ steps.slot.outputs.slot }}" \
            -o preview.json
          echo "Prévia recebida:"
          cat preview.json | head -c 400; echo

          node -e '
            const fs = require("fs");
            const p = JSON.parse(fs.readFileSync("preview.json", "utf8"));
            const props = {
              title: p.title || "Dr. Libertad",
              slides: Array.isArray(p.slides) ? p.slides : [],
              accentWords: Array.isArray(p.accentWords) ? p.accentWords : [],
              cta: p.cta || "",
              kw: p.kw || "LIBERTAD",
              ed: p.ed || "000",
              img: p.illustration || undefined,
              cat: p.cat || "freedom",
              // Termos de busca de footage gerados pelo Claude (no tema do post).
              videoQueries: Array.isArray(p.videoQueries) ? p.videoQueries : [],
              // Trilha embutida — usa bed.wav ou bed.mp3 se existir no repo
              // (camada de criação opcional; sem ela o Reel renderiza mudo).
              music: fs.existsSync("public/music/bed.wav") ? "music/bed.wav"
                   : fs.existsSync("public/music/bed.mp3") ? "music/bed.mp3" : undefined
            };
            // Caption do reel: usa a legenda gerada pelo Claude (mesma do carrossel)
            fs.writeFileSync("caption.txt", p.caption || p.title || "");
            fs.writeFileSync("reel-props.json", JSON.stringify(props));
            console.log("REEL_PROPS montado:", JSON.stringify(props).slice(0, 300));
          '

      # 5b. Busca footage REAL de banco (Pexels, grátis) p/ o tema do dia → clips
      #     de fundo (movimento real, 2-3 cenas). NÃO-FATAL: sem PEXELS_API_KEY ou
      #     sem match, segue sem clips e o Reel usa a ilustração estática.
      - name: Buscar footage (Pexels) → clipes de fundo
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          FOOTAGE_NUM_CLIPS: "4"
        run: node scripts/fetch-footage.mjs --props=reel-props.json

      # 6. Renderiza o Reel para out/reel.mp4
      - name: Renderizar Reel (Remotion)
        run: node scripts/render-reel.mjs --props=reel-props.json

      # 7. Sobe o mp4 para o Vercel Blob e captura a URL pública (stdout)
      - name: Upload do mp4 para o Vercel Blob
        id: upload
        env:
          BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}
        run: |
          URL=$(node scripts/upload-blob.mjs)
          echo "URL do vídeo: $URL"
          echo "video_url=$URL" >> "$GITHUB_OUTPUT"

      # 7b. Modo preview: no disparo manual com publish=no, NÃO publica — só
      #     expõe a URL do mp4 p/ você revisar a criação antes de ir pro ar.
      #     O cron (schedule) sempre publica.
      - name: Preview (sem publicar)
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.publish != 'yes' }}
        run: |
          echo "::notice title=Reel pronto (preview, não publicado)::${{ steps.upload.outputs.video_url }}"
          echo "Preview do Reel (NÃO publicado): ${{ steps.upload.outputs.video_url }}"

      # 8. Publica o Reel no Instagram via /api/publish-reel (faz o polling de status).
      #     Roda no cron (schedule) ou no disparo manual com publish=yes.
      - name: Publicar Reel no Instagram
        if: ${{ github.event_name == 'schedule' || github.event.inputs.publish == 'yes' }}
        run: |
          RESP=$(curl -sS --max-time 280 -G \
            -w $'\nHTTP_CODE:%{http_code}' \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            --data-urlencode "video=${{ steps.upload.outputs.video_url }}" \
            --data-urlencode "caption@caption.txt" \
            --data-urlencode "slot=${{ steps.slot.outputs.slot }}" \
            "https://www.drlibertad.com/api/publish-reel")
          echo "Resposta do servidor:"
          echo "$RESP"
          echo "$RESP" | grep -q '"ok":true' || { echo "::error::Publicação falhou"; exit 1; }
          echo "Reel publicado."
